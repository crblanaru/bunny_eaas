services:
  api:
    build: .
    container_name: fastapi-application
    environment: &all_env
      PORT: 80
      APP_ENV: dev
    ports:
      - '80:80'
#    volumes:
#      - application_files:/tmp/test
  cache:
    environment: *all_env
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
#volumes:
#  application_files:
